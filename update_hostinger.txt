# Perintah Update untuk Hostinger (setelah fix routing)

# 1. Pull update terbaru dari GitHub
git pull origin main

# 2. Update dependencies (sekarang support PHP 8.3)
composer install --no-dev --optimize-autoloader

# 3. Clear semua cache
php artisan config:clear
php artisan route:clear
php artisan view:clear
php artisan cache:clear

# 4. Cache ulang untuk production
php artisan config:cache
php artisan route:cache
php artisan view:cache

# 5. Test akses
# Buka https://wms.msapt.co.id (seharusnya redirect ke login jika belum login)
# Login dengan admin@msa.com / password
# Seharusnya redirect ke dashboard yang sesuai dengan role user
